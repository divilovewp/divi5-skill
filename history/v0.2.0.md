# DIVI5 JSON Generation Skill — Version 0.2.0

> **How to Use This Skill**
> Attach this file to any conversation where you need to generate DIVI5 page layout JSON files. Read it fully before writing any JSON. All rules are derived from: (a) the original DIVI5-SKILL.md v0.1.0, and (b) direct inspection of the DIVI5 theme source code — including `PortabilityPost.php`, `Layout.php`, `HeadingModule.php`, module JSON definitions, and conversion outlines.

---

## Table of Contents

1. [Overview & Key Principles](#1-overview--key-principles)
2. [Top-Level JSON Structure](#2-top-level-json-structure)
3. [Import & Export — How It Works](#3-import--export--how-it-works)
4. [Module Reference](#4-module-reference)
5. [Nesting Rules & Hierarchy](#5-nesting-rules--hierarchy)
6. [Row Layout: The Flexbox System](#6-row-layout-the-flexbox-system)
7. [Styling Properties](#7-styling-properties)
8. [DIVI5 Variables / Design Tokens](#8-divi5-variables--design-tokens)
9. [Real-World Patterns](#9-real-world-patterns)
10. [Step-by-Step Build Workflow](#10-step-by-step-build-workflow)
11. [Programmatic JSON Generation (Python)](#11-programmatic-json-generation-python)
12. [Common Mistakes & Fixes](#12-common-mistakes--fixes)
13. [Validation Checklist](#13-validation-checklist)
14. [Quick Reference Card](#14-quick-reference-card)

---

## 1. Overview & Key Principles

### What is DIVI5 JSON?

DIVI5 is a WordPress page builder by Elegant Themes. Pages are stored as WordPress Block Editor (Gutenberg) markup embedded inside a JSON file. The JSON is imported via DIVI5's Import & Export interface, instantly recreating the full layout.

### Non-Negotiable Rules — Always Follow These

1. **Every page content string is wrapped in `<!-- wp:divi/placeholder -->...<!-- /wp:divi/placeholder -->`.**
2. **Hierarchy is always: Section → Row → Column → Module(s).** Never skip levels.
3. **`builderVersion: "5.0.1"` must be on every single module** — section, row, column, and all content modules.
4. **Self-closing modules** end with ` /-->` (space + slash): `<!-- wp:divi/heading {...} /-->`.
5. **Container modules** (section, row, column, group, etc.) use open + close pairs.
6. **Row flex layout is driven by `flexColumnStructure` + `layout.*.value.flexWrap`** — NOT by a `display: flex` property. Divi applies flex automatically.
7. **Column widths are set via `module.decoration.sizing.*.value.flexType`** using the `N_24` fraction system.
8. **`flexColumnStructure` count must exactly match the number of `divi/column` children** in that row.
9. **Heading `title.innerContent` is plain text only** — no HTML tags. The heading tag (`h1`, `h2`, etc.) is controlled by `title.decoration.font.font.*.value.headingLevel`.
10. **Rich text content** (text modules, blurb body, testimonial content) must HTML-encode `<` as `\u003c` and `>` as `\u003e`.
11. **Never include `groupPreset`** — it references site-specific preset IDs that won't exist on other installations.
12. **Spacing is optional** — DIVI5 applies default padding when none is specified. Only add explicit spacing when overriding defaults.
13. **The `data` field value is a plain string** — not a nested object with `post_content`, `post_title`, etc. (Those sub-keys belong to the `post_type` portability context, not `et_builder`.)

---

## 2. Top-Level JSON Structure

```json
{
  "context": "et_builder",
  "data": {
    "11": "<!-- wp:divi/placeholder -->...PAGE_CONTENT...<!-- /wp:divi/placeholder -->"
  },
  "presets": null,
  "global_colors": [],
  "global_variables": [],
  "canvases": {"local": [], "global": []},
  "images": [],
  "thumbnails": []
}
```

### Field Descriptions

| Key | Required | Notes |
|-----|----------|-------|
| `context` | Yes | Always `"et_builder"` — validated server-side in `PortabilityPost.php` |
| `data` | Yes | Object with **one key** (any integer string, e.g. `"11"`). Value is the full page markup string. |
| `presets` | No | `null` for pages without saved presets |
| `global_colors` | No | Array of `[id, {color, status, label}]` tuples |
| `global_variables` | No | Array of variable objects |
| `canvases` | No | `{"local": [], "global": []}` for standard exports |
| `images` | No | `[]` |
| `thumbnails` | No | `[]` |

### Critical: `data` Value Format

The value of the `data["11"]` key must be a **plain string** containing the Gutenberg markup. Discovered by tracing `PortabilityPost.php` → `get_layout_content()`:

```php
// PortabilityPost.php line 1251
if ( is_string( $first_data ) || ! array_key_exists( 'post_content', $first_data ) ) {
    $layout_content = $first_data;  // string path used by et_builder context
```

❌ **Wrong** (this is the `post_type` format — for layout libraries, not pages):
```json
"data": {
  "1": {
    "post_content": "<!-- wp:divi/... -->",
    "post_title": "My Page",
    "post_type": "page"
  }
}
```

✅ **Correct** (raw string for `et_builder` context):
```json
"data": {
  "11": "<!-- wp:divi/placeholder --><!-- wp:divi/section...<!-- /wp:divi/placeholder -->"
}
```

---

## 3. Import & Export — How It Works

### Import Path

When a JSON file is uploaded via DIVI's Import & Export UI:

1. WordPress calls the REST endpoint handled by `PortabilityController.php`
2. The controller validates `context === "et_builder"`
3. `PortabilityPost.php::import()` parses the file
4. `get_layout_content()` reads `data["11"]` as a string (the raw markup)
5. The markup is written to the current page's `post_content`
6. DIVI5 renders the Gutenberg blocks on next page load

### Context Validation

The import will hard-fail with `"This file should not be imported in this context."` if the JSON `context` field does not match the registered portability instance context. Always use `"et_builder"` for page imports.

---

## 4. Module Reference

### 4.1 Layout Modules (Structural)

---

#### `divi/section`

Top-level container. Every page needs at least one.

**Self-closing?** No.

```json
{
  "module": {
    "meta": {
      "adminLabel": {"desktop": {"value": "Hero Section"}}
    },
    "decoration": {
      "background": {
        "desktop": {"value": {"color": "#0f172a"}}
      },
      "spacing": {
        "desktop": {"value": {"padding": {"top": "80px", "bottom": "80px"}}}
      }
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/row`

Horizontal container inside a section. Defines column arrangement via the flex system.

**Self-closing?** No.

```json
{
  "module": {
    "advanced": {
      "flexColumnStructure": {
        "desktop": {"value": "equal-columns_3"}
      }
    },
    "decoration": {
      "layout": {
        "desktop": {"value": {"flexWrap": "nowrap"}},
        "phone": {"value": {"flexWrap": "wrap"}},
        "phoneWide": {"value": {"flexWrap": "wrap"}}
      },
      "sizing": {
        "desktop": {"value": {"maxWidth": "1280px"}}
      }
    }
  },
  "builderVersion": "5.0.1"
}
```

**`flexColumnStructure` values:**

| Value | Columns |
|-------|---------|
| `"equal-columns_1"` | 1 column (full width) |
| `"equal-columns_2"` | 2 equal columns |
| `"equal-columns_3"` | 3 equal columns |
| `"equal-columns_4"` | 4 equal columns |

> **Source:** Confirmed via `row/conversion-outline.json`: `"layout_display": "module.decoration.layout.*.display"` and `"column_structure": "module.advanced.columnStructure.*"`. The flex layout is triggered by `flexColumnStructure` being set — Divi handles `display: flex` internally.

---

#### `divi/column`

A column inside a row. Contains content modules.

**Self-closing?** No.

```json
{
  "module": {
    "decoration": {
      "sizing": {
        "desktop": {"value": {"flexType": "8_24"}},
        "phone": {"value": {"flexType": "24_24"}},
        "phoneWide": {"value": {"flexType": "24_24"}}
      }
    }
  },
  "builderVersion": "5.0.1"
}
```

**`flexType` cheat sheet (column width as fraction of 24):**

| flexType | Width | Use case |
|----------|-------|----------|
| `"24_24"` | 100% | Full width / single column |
| `"16_24"` | 66.7% | Two-thirds |
| `"12_24"` | 50% | Half |
| `"8_24"` | 33.3% | One-third |
| `"6_24"` | 25% | One-quarter |
| `"18_24"` | 75% | Three-quarters |
| `"4_24"` | 16.7% | Narrow sidebar |

---

#### `divi/group`

A generic `div` wrapper for grouping modules inside a column.

**Self-closing?** No.

```json
{"builderVersion": "5.0.1"}
```

---

#### `divi/group-carousel`

Carousel of `divi/group` slides. Each child `group` is one slide.

**Self-closing?** No.

---

### 4.2 Content Modules

---

#### `divi/heading`

Renders a standalone heading element.

**Self-closing?** Yes (`/-->`).

> **Source:** Confirmed in `heading/module.json`: `"inlineEditor": "plainText"` — title value MUST be plain text. The HTML tag is set via `headingLevel` in `title.decoration.font.font.*.value`.

```json
{
  "title": {
    "innerContent": {
      "desktop": {"value": "Your Heading Text Here"}
    },
    "decoration": {
      "font": {
        "font": {
          "desktop": {
            "value": {
              "headingLevel": "h2",
              "size": "48px",
              "weight": "800",
              "color": "#FFFFFF",
              "textAlign": "center"
            }
          }
        }
      }
    }
  },
  "builderVersion": "5.0.1"
}
```

**`headingLevel` options:** `"h1"`, `"h2"`, `"h3"`, `"h4"`, `"h5"`, `"h6"`

❌ **Wrong — never put HTML tags in heading value:**
```json
"innerContent": {"desktop": {"value": "<h2>My Title</h2>"}}
```
✅ **Correct — plain text only:**
```json
"innerContent": {"desktop": {"value": "My Title"}}
```

---

#### `divi/text`

Renders a block of HTML content (paragraphs, lists, etc.).

**Self-closing?** Yes.

> **Source:** `text/module.json` uses `"inlineEditor": "richtext"` — HTML content is expected and must be unicode-escaped.

```json
{
  "content": {
    "innerContent": {
      "desktop": {"value": "\u003cp\u003eYour paragraph text here.\u003c/p\u003e"}
    },
    "decoration": {
      "bodyFont": {
        "body": {
          "font": {
            "desktop": {
              "value": {
                "color": "#e2e8f0",
                "size": "18px",
                "textAlign": "center"
              }
            }
          }
        }
      }
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/button`

Renders a styled CTA button.

**Self-closing?** Yes.

> **Source:** Button uses `linkUrl` and `linkTarget` — NOT `url`/`newWindow` (those are Divi 4 shortcode param names).

```json
{
  "button": {
    "innerContent": {
      "desktop": {
        "value": {
          "text": "Get Started",
          "linkUrl": "/contact",
          "linkTarget": "off"
        }
      }
    }
  },
  "module": {
    "decoration": {
      "background": {"desktop": {"value": {"color": "#2563eb"}}},
      "border": {
        "desktop": {"value": {"radius": {"topLeft": "8px", "topRight": "8px", "bottomLeft": "8px", "bottomRight": "8px", "sync": "on"}}}
      },
      "spacing": {
        "desktop": {"value": {"padding": {"top": "18px", "bottom": "18px", "left": "48px", "right": "48px"}}}
      },
      "layout": {"desktop": {"value": {"justifyContent": "center"}}}
    }
  },
  "builderVersion": "5.0.1"
}
```

**`linkTarget`:** `"off"` = same tab, `"on"` = new tab.

---

#### `divi/image`

Displays an image.

**Self-closing?** Yes.

```json
{
  "image": {
    "innerContent": {
      "desktop": {
        "value": {
          "src": "https://example.com/image.jpg",
          "alt": "Description of image"
        }
      }
    }
  },
  "module": {
    "decoration": {
      "sizing": {"desktop": {"value": {"width": "100%", "maxWidth": "500px"}}},
      "border": {
        "desktop": {"value": {"radius": {"topLeft": "1rem", "topRight": "1rem", "bottomLeft": "1rem", "bottomRight": "1rem", "sync": "on"}}}
      }
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/blurb`

Icon/image + title + content. Perfect for feature cards.

**Self-closing?** Yes.

> **Source:** Blurb `title.innerContent` uses `{"text": "..."}` object format — NOT a plain string and NOT an HTML string.

```json
{
  "imageIcon": {
    "innerContent": {
      "desktop": {"value": {"src": "https://example.com/icon.svg"}}
    }
  },
  "title": {
    "innerContent": {
      "desktop": {"value": {"text": "Feature Title"}}
    },
    "decoration": {
      "font": {
        "font": {
          "desktop": {"value": {"headingLevel": "h3", "size": "22px", "weight": "700", "color": "#1F2937", "textAlign": "center"}}
        }
      }
    }
  },
  "content": {
    "innerContent": {
      "desktop": {"value": "\u003cp\u003eFeature description here.\u003c/p\u003e"}
    },
    "decoration": {
      "bodyFont": {"body": {"font": {"desktop": {"value": {"color": "#374151", "textAlign": "center"}}}}}
    }
  },
  "module": {
    "decoration": {
      "background": {"desktop": {"value": {"color": "#FFFFFF"}}},
      "border": {
        "desktop": {"value": {"radius": {"topLeft": "16px", "topRight": "16px", "bottomLeft": "16px", "bottomRight": "16px", "sync": "on"}}}
      },
      "spacing": {"desktop": {"value": {"padding": {"top": "40px", "bottom": "40px", "left": "32px", "right": "32px"}}}}
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/cta` (Call to Action)

Title + body + button, combined.

**Self-closing?** Yes.

```json
{
  "title": {"innerContent": {"desktop": {"value": "Ready to Begin?"}}},
  "content": {"innerContent": {"desktop": {"value": "\u003cp\u003eJoin thousands of customers.\u003c/p\u003e"}}},
  "button": {
    "innerContent": {"desktop": {"value": {"text": "Get Started", "linkUrl": "/signup", "linkTarget": "off"}}}
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/testimonial`

Testimonial quote with author and optional portrait.

**Self-closing?** Yes.

```json
{
  "content": {
    "innerContent": {
      "desktop": {"value": "\u003cp\u003eThis service transformed our business entirely.\u003c/p\u003e"}
    }
  },
  "author": {
    "innerContent": {"desktop": {"value": "Jane Smith"}},
    "decoration": {
      "font": {"font": {"desktop": {"value": {"color": "#F59E0B", "weight": "700"}}}}
    }
  },
  "jobTitle": {
    "innerContent": {"desktop": {"value": "CEO, Acme Corp"}}
  },
  "portrait": {
    "innerContent": {"desktop": {"value": {"url": "https://example.com/portrait.jpg"}}}
  },
  "module": {
    "decoration": {
      "background": {"desktop": {"value": {"color": "#374151"}}},
      "border": {"desktop": {"value": {"radius": {"topLeft": "16px", "topRight": "16px", "bottomLeft": "16px", "bottomRight": "16px", "sync": "on"}}}},
      "spacing": {"desktop": {"value": {"padding": {"top": "40px", "bottom": "40px", "left": "40px", "right": "40px"}}}}
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/number-counter`

Animated number counter.

**Self-closing?** Yes.

> **Source:** `enablePercentSign` controls whether a % symbol appears after the number. Uses `"on"` / `"off"` string values.

```json
{
  "number": {
    "innerContent": {"desktop": {"value": "98"}},
    "decoration": {
      "font": {"font": {"desktop": {"value": {"size": "64px", "weight": "800", "color": "#6B21A8", "textAlign": "center"}}}}
    },
    "advanced": {"enablePercentSign": {"desktop": {"value": "on"}}}
  },
  "title": {
    "innerContent": {"desktop": {"value": "Client Satisfaction"}},
    "decoration": {
      "font": {"font": {"desktop": {"value": {"headingLevel": "h4", "size": "18px", "weight": "700", "color": "#1F2937", "textAlign": "center"}}}}
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/circle-counter`

Circular progress indicator.

**Self-closing?** Yes.

```json
{
  "title": {"innerContent": {"desktop": {"value": "Satisfaction"}}},
  "number": {"innerContent": {"desktop": {"value": "98"}}},
  "builderVersion": "5.0.1"
}
```

---

#### `divi/counters` + `divi/counter` (Bar Counters)

Horizontal progress bars. `counters` is NOT self-closing. `counter` IS self-closing.

```json
// counters (parent):
{
  "barProgress": {
    "advanced": {"usePercentages": {"desktop": {"value": "on"}}}
  },
  "builderVersion": "5.0.1"
}

// counter (child):
{
  "title": {"innerContent": {"desktop": {"value": "Skill Name"}}},
  "barProgress": {"innerContent": {"desktop": {"value": "75"}}},
  "builderVersion": "5.0.1"
}
```

---

#### `divi/video`

Embeds a video.

**Self-closing?** Yes.

```json
{
  "video": {
    "innerContent": {
      "desktop": {"value": {"src": "https://www.youtube.com/watch?v=XXXXX"}}
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/code`

Renders raw HTML/CSS/JS.

**Self-closing?** Yes.

```json
{
  "content": {
    "innerContent": {
      "desktop": {"value": "\u003cstyle\u003e.custom { color: red; }\u003c/style\u003e"}
    }
  },
  "module": {
    "decoration": {
      "zIndex": {"desktop": {"value": "-10"}}
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/divider`

A horizontal rule / decorative divider.

**Self-closing?** Yes. Minimal: `{"builderVersion": "5.0.1"}`

---

#### `divi/icon`

Renders a single Divi icon.

**Self-closing?** Yes. Minimal: `{"builderVersion": "5.0.1"}`

---

#### `divi/icon-list` + `divi/icon-list-item`

Icon list. `icon-list` is NOT self-closing. `icon-list-item` IS self-closing.

```json
// icon-list-item:
{
  "content": {"innerContent": {"desktop": {"value": "List item text"}}},
  "icon": {
    "innerContent": {
      "desktop": {"value": {"unicode": "&#x21;", "type": "divi", "weight": "400", "target": "off"}}
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/link`

A text hyperlink.

**Self-closing?** Yes.

```json
{
  "content": {
    "innerContent": {
      "desktop": {"value": {"text": "Click here", "linkUrl": "/page", "linkTarget": "off"}}
    }
  },
  "builderVersion": "5.0.1"
}
```

---

#### `divi/team-member`

Team member card with photo, name, position.

**Self-closing?** Yes. Tag is `divi/team-member` (not `divi/person`).

```json
{
  "name": {"innerContent": {"desktop": {"value": "Jane Smith"}}},
  "position": {"innerContent": {"desktop": {"value": "Lead Designer"}}},
  "image": {
    "innerContent": {"desktop": {"value": {"url": "https://example.com/photo.jpg"}}}
  },
  "content": {"innerContent": {"desktop": {"value": "\u003cp\u003eBio text here.\u003c/p\u003e"}}},
  "builderVersion": "5.0.1"
}
```

---

### 4.3 Interactive / Form Modules

---

#### `divi/accordion` + `divi/accordion-item`

Expandable FAQ items. `accordion` is NOT self-closing. `accordion-item` IS self-closing.

```json
// accordion-item:
{
  "module": {
    "advanced": {"open": {"desktop": {"value": "on"}}}
  },
  "title": {"innerContent": {"desktop": {"value": "Question text?"}}},
  "content": {"innerContent": {"desktop": {"value": "\u003cp\u003eAnswer text.\u003c/p\u003e"}}},
  "builderVersion": "5.0.1"
}
```

---

#### `divi/toggle`

Single expand/collapse toggle.

**Self-closing?** Yes.

```json
{
  "title": {"innerContent": {"desktop": {"value": "Toggle Title"}}},
  "content": {"innerContent": {"desktop": {"value": "\u003cp\u003eContent here.\u003c/p\u003e"}}},
  "builderVersion": "5.0.1"
}
```

---

#### `divi/tabs` + `divi/tab`

Tabbed panels. `tabs` NOT self-closing. `tab` IS self-closing.

```json
// tab:
{
  "title": {"innerContent": {"desktop": {"value": "Tab Label"}}},
  "content": {"innerContent": {"desktop": {"value": "\u003cp\u003eTab content.\u003c/p\u003e"}}},
  "builderVersion": "5.0.1"
}
```

---

#### `divi/contact-form` + `divi/contact-field`

`contact-form` NOT self-closing. `contact-field` IS self-closing.

```json
// contact-form:
{
  "module": {
    "advanced": {"uniqueId": {"desktop": {"value": "9d29eac0-fe91-4dad-a40d-350e889da677"}}}
  },
  "builderVersion": "5.0.1"
}

// contact-field:
{
  "module": {
    "decoration": {"sizing": {"desktop": {"value": {"flexType": "12_24"}}}}
  },
  "fieldItem": {
    "advanced": {
      "fullwidth": {"desktop": {"value": "on"}},
      "id": {"desktop": {"value": "Name"}},
      "type": {"desktop": {"value": "input"}}
    },
    "innerContent": {"desktop": {"value": "Name"}}
  },
  "builderVersion": "5.0.1"
}
```

**Field types:** `"input"` (text), `"email"`, `"text"` (textarea), `"checkbox"`, `"select"`

---

#### `divi/signup`

Email subscription form.

**Self-closing?** Yes.

```json
{
  "title": {"innerContent": {"desktop": {"value": "Subscribe"}}},
  "content": {"innerContent": {"desktop": {"value": "\u003cp\u003eGet updates.\u003c/p\u003e"}}},
  "builderVersion": "5.0.1"
}
```

---

### 4.4 Pricing & Social

---

#### `divi/pricing-tables` + `divi/pricing-table`

`pricing-tables` NOT self-closing. `pricing-table` IS self-closing.

```json
// pricing-table:
{
  "currencyFrequency": {
    "innerContent": {"desktop": {"value": {"currency": "$", "frequency": "/month"}}}
  },
  "title": {"innerContent": {"desktop": {"value": "Pro Plan"}}},
  "subtitle": {"innerContent": {"desktop": {"value": "For growing teams"}}},
  "price": {"innerContent": {"desktop": {"value": "99"}}},
  "content": {"innerContent": {"desktop": {"value": "+ Feature A\n+ Feature B\n- Feature C"}}},
  "builderVersion": "5.0.1"
}
```

---

#### `divi/social-media-follow` + `divi/social-media-follow-network`

`social-media-follow` NOT self-closing. Network items ARE self-closing.

```json
// network item:
{
  "module": {
    "decoration": {"background": {"desktop": {"value": {"color": "#1877f2"}}}}
  },
  "socialNetwork": {
    "innerContent": {
      "desktop": {"value": {"title": "facebook", "label": "Facebook"}}
    }
  },
  "builderVersion": "5.0.1"
}
```

---

### 4.5 Media Modules

| Module | Self-closing | Key attribute |
|--------|-------------|---------------|
| `divi/gallery` | Yes | `galleryGrid.decoration.layout.*.value.gridColumnCount` |
| `divi/slider` + `divi/slide` | Container/Yes | `slide.title`, `slide.button`, `slide.content` |
| `divi/video-slider` + `divi/video-slider-item` | Container/Yes | `video.innerContent.*.value.src` |
| `divi/lottie` | Yes | `lottie.innerContent.*.value.src` |
| `divi/audio` | Yes | `artistName`, `title` innerContent |
| `divi/before-after-image` | Yes | `beforeImage`, `afterImage` innerContent |

---

### 4.6 Dynamic WordPress Modules

These require WordPress content (posts, CPTs) to be present on the target site.

| Tag | Description |
|-----|-------------|
| `divi/blog` | Blog post grid |
| `divi/post-carousel` | Posts in carousel |
| `divi/post-slider` | Posts in slider |
| `divi/post-title` | For single post templates |
| `divi/portfolio` | Divi Project CPT |
| `divi/filterable-portfolio` | With category filters |
| `divi/comments` | WordPress comments |
| `divi/pagination` | Loop pagination |

---

### 4.7 Misc Modules

| Tag | Description | Self-closing |
|-----|-------------|--------------|
| `divi/countdown-timer` | Countdown to a date | Yes |
| `divi/map` | Google Maps embed | Yes |
| `divi/menu` | Navigation menu | Yes |
| `divi/search` | Search box | Yes |
| `divi/sidebar` | Widget sidebar | Yes |
| `divi/login` | Login form | Yes |
| `divi/dropdown` | Dropdown content | Yes |
| `divi/canvas-portal` | Off-page canvas display | Yes |
| `divi/fullwidth-header` | Full-width hero | Yes |
| `divi/post-nav` | Previous/next navigation | Yes |

---

## 5. Nesting Rules & Hierarchy

### 5.1 The Fundamental Hierarchy

```
placeholder wrapper
  └── Section
        └── Row
              └── Column
                    └── Module(s) or Group
                          └── Module(s) [if inside Group]
```

**Never place a module directly in a section or row.** Always follow: Section → Row → Column → Module.

### 5.2 Advanced Nesting

DIVI5 supports:
- **Nested Rows**: A `row` can live inside a `column` (which is inside an outer row).
- **Groups**: A `group` can contain modules and/or a nested row structure.
- **Group Carousel**: `group-carousel` → `group` (one per slide) → modules.

```
Section
  └── Row (2-col)
        ├── Left Column
        │     └── Group
        │           ├── Heading
        │           └── Button
        └── Right Column
              └── Row (1-col)     ← nested row
                    └── Column
                          └── Blurb
```

### 5.3 Parent–Child Module Rules

| Parent | Valid Children |
|--------|----------------|
| `section` | `row`, `global-layout` |
| `row` | `column` only |
| `column` | `group`, `group-carousel`, any content module, nested `row` |
| `group` | any content module, nested `row` |
| `group-carousel` | `group` |
| `accordion` | `accordion-item` |
| `counters` | `counter` |
| `contact-form` | `contact-field` |
| `pricing-tables` | `pricing-table` |
| `slider` | `slide` |
| `tabs` | `tab` |
| `social-media-follow` | `social-media-follow-network` |
| `icon-list` | `icon-list-item` |
| `video-slider` | `video-slider-item` |

---

## 6. Row Layout: The Flexbox System

This section documents exactly how Divi 5 rows achieve multi-column flex layout. This was validated by reading `Layout.php`, `row/module.json`, and `row/conversion-outline.json` directly.

### 6.1 How Row Flex Actually Works

**Divi applies `display: flex` automatically to rows** when `flexColumnStructure` is set. You do NOT set `display: flex` yourself. The `module.decoration.layout` on a row controls `flexWrap` and other flex child properties — not the `display` value.

The PHP class `Layout.php` reads `module.decoration.layout.*.value` and outputs CSS like `flex-direction`, `flex-wrap`, `justify-content`, etc. The display mode itself is handled by Divi's row renderer.

### 6.2 Required Row Attributes for Multi-Column Layout

Every multi-column row MUST have:

```json
{
  "module": {
    "advanced": {
      "flexColumnStructure": {
        "desktop": {"value": "equal-columns_3"}
      }
    },
    "decoration": {
      "layout": {
        "desktop": {"value": {"flexWrap": "nowrap"}},
        "phone": {"value": {"flexWrap": "wrap"}},
        "phoneWide": {"value": {"flexWrap": "wrap"}}
      }
    }
  },
  "builderVersion": "5.0.1"
}
```

### 6.3 Column Width Alignment Rule

The sum of all column `flexType` values in a row should equal 24. Examples:

| Layout | Column flexTypes |
|--------|-----------------|
| 1 column | `24_24` |
| 2 equal columns | `12_24` + `12_24` |
| 3 equal columns | `8_24` + `8_24` + `8_24` |
| 4 equal columns | `6_24` × 4 |
| 2/3 + 1/3 split | `16_24` + `8_24` |
| 3/4 + 1/4 split | `18_24` + `6_24` |

### 6.4 Common Layout Mistakes

❌ Setting `display: "flex"` on rows:
```json
"layout": {"desktop": {"value": {"display": "flex"}}}
```
This does NOT work. Divi ignores this on rows.

❌ Setting `"layout": {"desktop": {"value": "flex"}}` (string, not object):
```json
"layout": {"desktop": {"value": "flex"}}
```
This is also invalid.

✅ Correct: `flexColumnStructure` + `flexWrap` in layout:
```json
"advanced": {"flexColumnStructure": {"desktop": {"value": "equal-columns_3"}}},
"decoration": {"layout": {"desktop": {"value": {"flexWrap": "nowrap"}}}}
```

---

## 7. Styling Properties

### 7.1 Background

```json
"background": {
  "desktop": {
    "value": {
      "color": "#0f172a",
      "gradient": {
        "enabled": "on",
        "type": "linear",
        "direction": "135deg",
        "stops": [
          {"position": 0, "color": "#0f172a"},
          {"position": 100, "color": "#1e293b"}
        ]
      }
    }
  }
}
```

**Gradient types:** `"linear"`, `"radial"`, `"circular"`

### 7.2 Spacing (Padding & Margin)

```json
"spacing": {
  "desktop": {
    "value": {
      "padding": {
        "top": "80px",
        "bottom": "80px",
        "left": "20px",
        "right": "20px",
        "syncVertical": "off",
        "syncHorizontal": "off"
      },
      "margin": {"top": "0px", "bottom": "40px"}
    }
  },
  "tablet": {"value": {"padding": {"top": "60px", "bottom": "60px"}}},
  "phone": {"value": {"padding": {"top": "40px", "bottom": "40px"}}}
}
```

### 7.3 Sizing

```json
"sizing": {
  "desktop": {
    "value": {
      "width": "100%",
      "maxWidth": "1280px",
      "minHeight": "100vh",
      "flexType": "12_24"
    }
  }
}
```

### 7.4 Border

```json
"border": {
  "desktop": {
    "value": {
      "styles": {
        "all": {"width": "1px", "color": "rgba(255,255,255,0.1)", "style": "solid"}
      },
      "radius": {
        "topLeft": "16px",
        "topRight": "16px",
        "bottomLeft": "16px",
        "bottomRight": "16px",
        "sync": "on"
      }
    }
  }
}
```

### 7.5 Box Shadow

```json
"boxShadow": {
  "desktop": {
    "value": {
      "horizontal": "0px",
      "vertical": "9px",
      "blur": "15px",
      "spread": "0px",
      "position": "outer",
      "color": "#2563eb"
    }
  }
}
```

### 7.6 Layout (on Modules / Columns — NOT Rows)

The `layout` property controls the flex behaviour of a module's *interior* — useful for centering column content or setting flex direction inside a column or group. On rows, only `flexWrap` is relevant (see Section 6).

```json
"layout": {
  "desktop": {
    "value": {
      "justifyContent": "center",
      "alignItems": "center",
      "flexDirection": "column",
      "rowGap": "20px",
      "columnGap": "20px"
    }
  }
}
```

### 7.7 Typography

Typography lives under `content.decoration.bodyFont` (text modules) or `title.decoration.font` (heading/blurb/cta modules).

```json
"font": {
  "desktop": {
    "value": {
      "family": "Outfit",
      "size": "18px",
      "weight": "700",
      "style": ["uppercase"],
      "lineHeight": "1.6",
      "letterSpacing": "0.1em",
      "textAlign": "center",
      "color": "#e2e8f0",
      "headingLevel": "h2"
    }
  }
}
```

**Font weights:** `"100"` – `"900"`, `"normal"`, `"bold"`
**Font styles (array):** `"italic"`, `"uppercase"`, `"underline"`, `"line-through"`
**Text align:** `"left"`, `"center"`, `"right"`, `"justify"`

### 7.8 Z-Index

```json
"zIndex": {"desktop": {"value": "-10"}}
```

### 7.9 Responsive Breakpoints

| Key | Screen Width |
|-----|-------------|
| `desktop` | ≥ 981px (always required) |
| `tablet` | 768–980px |
| `phoneWide` | 480–767px |
| `phone` | < 480px |

**Always provide `desktop` values. Add other breakpoints only when behavior should differ.**

### 7.10 Admin Labels & Custom CSS Classes

```json
"module": {
  "meta": {
    "adminLabel": {"desktop": {"value": "Hero Section"}}
  },
  "advanced": {
    "htmlAttributes": {
      "class": {"desktop": {"value": "my-custom-class"}},
      "id": {"desktop": {"value": "hero-section"}}
    }
  }
}
```

---

## 8. DIVI5 Variables / Design Tokens

### 8.1 Variable Reference Syntax

**Color variable:**
```
$variable({"type":"color","value":{"name":"gcid-ID","settings":{}}})$
```

**Font size / numeric variable:**
```
$variable({"type":"content","value":{"name":"gvid-ID","settings":{}}})$
```

**Global font family variable:**
```
$variable({"type":"content","value":{"name":"--et_global_heading_font","settings":{}}})$
$variable({"type":"content","value":{"name":"--et_global_body_font","settings":{}}})$
```

> **Critical:** Always include `"settings":{}` inside the variable reference. Omitting it breaks the reference.

### 8.2 Using Variables in Module Attributes

```json
"background": {
  "desktop": {
    "value": {
      "color": "$variable({\"type\":\"color\",\"value\":{\"name\":\"gcid-primary-color\",\"settings\":{}}})$"
    }
  }
}
```

Inside a JSON string context, the inner quotes must be escaped with `\"`.

### 8.3 Defining Variables in the Export File

**`global_colors` format:**
```json
"global_colors": [
  ["gcid-primary-color", {"color": "#0f172a", "status": "active", "label": "Primary Color"}],
  ["gcid-body-color", {"color": "#e2e8f0", "status": "active", "label": "Body Text Color"}]
]
```

**`global_variables` format (font sizes):**
```json
"global_variables": [
  {
    "id": "gvid-1ndmhhes58",
    "label": "Big Heading",
    "value": "clamp(2.5rem, 6vw, 5rem)",
    "status": "active",
    "lastUpdated": "2026-02-23T10:04:11.536Z",
    "order": "1",
    "type": "numbers"
  }
]
```

**`global_variables` format (fonts):**
```json
{
  "id": "--et_global_heading_font",
  "label": "Heading Font",
  "value": "Inter",
  "groupKey": "fonts",
  "status": "active",
  "order": 1,
  "type": "fonts"
}
```

---

## 9. Real-World Patterns

### 9.1 Single-Column Centred Content Row

Used for section headers, CTA text, standalone headings.

```
Row (equal-columns_1, max-width 1280px)
  └── Column (24_24)
        └── Heading (centered)
        └── Text (centered, max-width 600px on column sizing)
        └── Button (centered, layout justifyContent: center)
```

### 9.2 Three-Feature Card Row

```
Row (equal-columns_3, nowrap desktop, wrap mobile)
  ├── Column (8_24 → 24_24 mobile)
  │     └── Blurb (icon + title + text, card background, rounded corners)
  ├── Column (8_24 → 24_24 mobile)
  │     └── Blurb (featured/highlighted card, different bg)
  └── Column (8_24 → 24_24 mobile)
        └── Blurb
```

### 9.3 Hero Section (Two-Column)

```
Section (full bg color, large padding)
  └── Row (equal-columns_2, nowrap → wrap+column-reverse on mobile)
        ├── Column (12_24 → 24_24 mobile, layout justifyContent: center)
        │     ├── Heading (h1, large, white)
        │     ├── Text (subtitle, body color)
        │     └── Button (primary color, centered)
        └── Column (12_24 → 24_24 mobile)
              └── Image (rounded, hero image)
```

**Row attrs for hero (reverse columns on mobile):**
```json
"layout": {
  "desktop": {"value": {"flexWrap": "nowrap"}},
  "phone": {"value": {"flexWrap": "wrap", "flexDirection": "column-reverse"}},
  "phoneWide": {"value": {"flexWrap": "wrap"}}
}
```

### 9.4 Testimonials Pattern

```
Section (dark bg)
  └── Row (1-col) → Heading ("What Our Clients Say")
  └── Row (equal-columns_2 or _3)
        └── Column(s) → Testimonial (card bg, rounded, author highlighted)
```

### 9.5 CTA Section Pattern

```
Section (accent bg color)
  └── Row (1-col)
        └── Column
              └── Group
                    ├── Heading (h2, centered)
                    ├── Text (subtitle, centered)
                    └── Button (contrast color, centered)
```

### 9.6 Stats Bar Pattern

```
Section (accent bg)
  └── Row (equal-columns_3, nowrap)
        ├── Column → number-counter (large number + label)
        ├── Column → number-counter
        └── Column → number-counter
```

### 9.7 Animated Background Orbs (Code Module Pattern)

Inject at the top of dark sections for decorative glowing orbs:

```
Column
  └── Code Module (HTML — orb divs with position:fixed)
  └── Code Module (CSS — .orb animation styles, zIndex: -10)
```

```html
<!-- HTML code module content: -->
<div style="position:fixed;top:0;left:0;width:100%;height:100%;pointer-events:none;overflow:hidden;z-index:-10;">
  <div class="orb orb-1"></div>
  <div class="orb orb-2"></div>
  <div class="orb orb-3"></div>
</div>
```

```css
/* CSS code module content: */
<style>
.orb { position:absolute; border-radius:50%; filter:blur(80px); z-index:-10; opacity:0.6; }
.orb-1 { width:300px; height:300px; background:#3b82f6; top:-50px; left:-100px; }
.orb-2 { width:400px; height:400px; background:#6366f1; bottom:10%; right:-100px; }
.orb-3 { width:200px; height:200px; background:#0ea5e9; top:40%; left:30%; opacity:0.3; }
</style>
```

---

## 10. Step-by-Step Build Workflow

### Step 1: Understand the Brief

Before writing any JSON, clarify:
- What sections are needed?
- How many columns per section?
- What are the brand colors and typography?
- Does the site use global design tokens?

### Step 2: Plan the Layout

Sketch the structure top-down:

```
[Section 1: Hero]            → 2-col row (text + image)
[Section 2: Stats]           → 3-col row (counters)
[Section 3: Features]        → 1-col header row + 3-col cards row
[Section 4: Testimonials]    → 1-col header + 2-col testimonial row
[Section 5: CTA]             → 1-col centered row
```

### Step 3: Select Modules

| Content type | Module |
|-------------|--------|
| Paragraph / rich text | `divi/text` |
| Standalone heading | `divi/heading` |
| Button only | `divi/button` |
| Image only | `divi/image` |
| Icon + title + text | `divi/blurb` |
| Title + body + button combined | `divi/cta` |
| Quote with attribution | `divi/testimonial` |
| Animated number | `divi/number-counter` |
| FAQ expandable | `divi/accordion` |
| Custom HTML/CSS | `divi/code` |
| Grouped modules | `divi/group` |
| Carousel of groups | `divi/group-carousel` |

### Step 4: Build Each Section

For each section, write in this order:
1. Open section tag with attrs
2. For each row:
   - Open row with `flexColumnStructure` + `layout.flexWrap`
   - For each column: open column with `flexType`, add modules, close column
   - Close row
3. Close section

### Step 5: Encode and Wrap

1. Build the complete markup string
2. Ensure HTML inside content values uses `\u003c` / `\u003e` encoding
3. Wrap entire content in `<!-- wp:divi/placeholder -->...<!-- /wp:divi/placeholder -->`
4. Place the string as the value of `data["11"]`
5. Build the root JSON object with all required keys

---

## 11. Programmatic JSON Generation (Python)

The most reliable way to generate valid DIVI5 JSON is using Python to build structured dicts and let `json.dumps()` handle all escaping. This avoids manual quote-escaping errors.

```python
import json

BV = "5.0.1"

def section(attrs, children):
    a = json.dumps(attrs, ensure_ascii=False, separators=(',', ':'))
    return f'<!-- wp:divi/section {a} -->\r\n{children}<!-- /wp:divi/section -->\r\n'

def row(attrs, children):
    a = json.dumps(attrs, ensure_ascii=False, separators=(',', ':'))
    return f'<!-- wp:divi/row {a} -->\r\n{children}<!-- /wp:divi/row -->\r\n'

def column(attrs, children):
    a = json.dumps(attrs, ensure_ascii=False, separators=(',', ':'))
    return f'<!-- wp:divi/column {a} -->\r\n{children}<!-- /wp:divi/column -->\r\n'

def module(name, attrs):
    a = json.dumps(attrs, ensure_ascii=False, separators=(',', ':'))
    return f'<!-- wp:divi/{name} {a} /-->\r\n'

def make_row(col_count, children_list, max_width="1280px"):
    """Build a flex row with equal columns. children_list is a list of markup strings."""
    flex_map = {1: "24_24", 2: "12_24", 3: "8_24", 4: "6_24"}
    flex = flex_map.get(col_count, "24_24")

    row_attrs = {
        "module": {
            "advanced": {
                "flexColumnStructure": {"desktop": {"value": f"equal-columns_{col_count}"}}
            },
            "decoration": {
                "layout": {
                    "desktop": {"value": {"flexWrap": "nowrap"}},
                    "phone": {"value": {"flexWrap": "wrap"}},
                    "phoneWide": {"value": {"flexWrap": "wrap"}}
                },
                "sizing": {"desktop": {"value": {"maxWidth": max_width}}}
            }
        },
        "builderVersion": BV
    }

    cols = ""
    for child in children_list:
        col_attrs = {
            "module": {
                "decoration": {
                    "sizing": {
                        "desktop": {"value": {"flexType": flex}},
                        "phone": {"value": {"flexType": "24_24"}},
                        "phoneWide": {"value": {"flexType": "24_24"}}
                    }
                }
            },
            "builderVersion": BV
        }
        cols += column(col_attrs, child)

    return row(row_attrs, cols)

def build_export(page_content):
    """Wrap markup in the root export JSON structure."""
    full_content = f"<!-- wp:divi/placeholder -->{page_content}<!-- /wp:divi/placeholder -->"
    return {
        "context": "et_builder",
        "data": {"11": full_content},
        "presets": None,
        "global_colors": [],
        "global_variables": [],
        "canvases": {"local": [], "global": []},
        "images": [],
        "thumbnails": []
    }

# --- Usage example ---
hero_content = (
    module("heading", {
        "title": {
            "innerContent": {"desktop": {"value": "Launch Faster. Grow Bolder."}},
            "decoration": {"font": {"font": {"desktop": {"value": {
                "headingLevel": "h1", "size": "72px", "weight": "800",
                "color": "#FFFFFF", "textAlign": "center"
            }}}}}
        },
        "builderVersion": BV
    }) +
    module("text", {
        "content": {
            "innerContent": {"desktop": {"value": "\u003cp\u003eYour subheading here.\u003c/p\u003e"}},
            "decoration": {"bodyFont": {"body": {"font": {"desktop": {"value": {
                "color": "#FFFFFF", "textAlign": "center", "size": "18px"
            }}}}}}
        },
        "builderVersion": BV
    })
)

hero_section = section({
    "module": {
        "decoration": {
            "background": {"desktop": {"value": {"color": "#6B21A8"}}},
            "spacing": {"desktop": {"value": {"padding": {"top": "120px", "bottom": "120px"}}}}
        }
    },
    "builderVersion": BV
}, make_row(1, [hero_content]))

export = build_export(hero_section)

with open("output.json", "w", encoding="utf-8") as f:
    json.dump(export, f, ensure_ascii=False, indent=2)
```

> **Note on HTML encoding:** When building content values in Python, write HTML tags normally in your source strings (e.g. `"\u003cp\u003eText\u003c/p\u003e"` directly, or as `"<p>Text</p>"`). When `json.dumps()` serializes the outer JSON, it correctly embeds the string. The important thing is that the final JSON file, when loaded and the page content string extracted, contains properly parseable block comment markup. Divi's PHP parser handles the content string correctly either way.

---

## 12. Common Mistakes & Fixes

### 12.1 Wrong `data` field structure

❌ Nested object (wrong for `et_builder` context):
```json
"data": {"1": {"post_content": "...", "post_title": "My Page"}}
```
✅ Plain string:
```json
"data": {"11": "<!-- wp:divi/placeholder -->...<!-- /wp:divi/placeholder -->"}
```

### 12.2 Missing `builderVersion`

❌ Any module without `"builderVersion": "5.0.1"` will fail to render or import.

✅ Every module, row, column, and section must include it.

### 12.3 Row Flex Not Working

❌ Setting `display: flex` or `display: "flex"` on rows — this is ignored by Divi.

✅ Set `flexColumnStructure` in `module.advanced` and `flexWrap` in `module.decoration.layout`:
```json
"advanced": {"flexColumnStructure": {"desktop": {"value": "equal-columns_3"}}},
"decoration": {"layout": {"desktop": {"value": {"flexWrap": "nowrap"}}}}
```

### 12.4 HTML Tags in Heading Content

❌ `"value": "<h2>My Title</h2>"` — tags will render literally as text.

✅ `"value": "My Title"` — plain text only. Set `headingLevel: "h2"` in the font decoration.

### 12.5 Self-Closing Syntax Error

❌ `<!-- wp:divi/text {...} -->` — this opens a container, never closes it.

✅ `<!-- wp:divi/text {...} /-->` — note the space before `/`.

### 12.6 Missing Closing Tags

Every container module (`section`, `row`, `column`, `group`, `accordion`, etc.) needs a matching close tag:
```
<!-- /wp:divi/section -->
<!-- /wp:divi/row -->
<!-- /wp:divi/column -->
```

### 12.7 `flexColumnStructure` Mismatch

If a row declares `equal-columns_3` but contains only 2 `divi/column` tags, Divi will render incorrectly. Count your columns and match the declaration exactly.

### 12.8 Missing `<!-- wp:divi/placeholder -->` Wrapper

The entire page content must be wrapped. Missing this causes import failure.

### 12.9 Wrong Button Attribute Names

❌ Divi 4 names: `url`, `newWindow`

✅ Divi 5 names: `linkUrl`, `linkTarget`

### 12.10 Missing `settings: {}` in Variable References

❌ `$variable({"type":"color","value":{"name":"gcid-primary-color"}})$`

✅ `$variable({"type":"color","value":{"name":"gcid-primary-color","settings":{}}})$`

### 12.11 Blurb Title Format

❌ Plain string: `"innerContent": {"desktop": {"value": "Feature Title"}}`

✅ Object with `text` key: `"innerContent": {"desktop": {"value": {"text": "Feature Title"}}}`

### 12.12 Invalid Module Nesting

❌ Module directly in section (no row/column)
❌ Module directly in row (no column)
❌ Section nested inside a column (sections cannot nest)

---

## 13. Validation Checklist

Before saving the final JSON, verify every item:

**Root structure:**
- [ ] `"context": "et_builder"` is present
- [ ] `"data"` has exactly one key (integer string)
- [ ] The `data` value is a plain string (not a nested object)

**Content string:**
- [ ] Starts with `<!-- wp:divi/placeholder -->`
- [ ] Ends with `<!-- /wp:divi/placeholder -->`
- [ ] Every section has at least one row
- [ ] Every row has at least one column
- [ ] No module is placed directly in a section or row

**Modules:**
- [ ] Every module has `"builderVersion": "5.0.1"`
- [ ] Every non-self-closing module has a matching close tag
- [ ] Every self-closing module ends with ` /-->` (space + slash)
- [ ] `flexColumnStructure` count matches actual column count per row
- [ ] Column `flexType` values are consistent with the layout intent
- [ ] Mobile breakpoints set for column widths (`phone`, `phoneWide`)
- [ ] Row `flexWrap: "wrap"` set on mobile breakpoints

**Content:**
- [ ] Heading `title.innerContent` values are plain text (no HTML tags)
- [ ] Rich text values in text/testimonial/blurb body modules are HTML-encoded (`\u003c`, `\u003e`)
- [ ] Blurb title uses `{"text": "..."}` object format
- [ ] Button uses `linkUrl` and `linkTarget` (not `url`/`newWindow`)
- [ ] Variable references include `"settings": {}`
- [ ] No `groupPreset` keys in any module

---

## 14. Quick Reference Card

### Module Type Identifiers

```
Structure:   divi/section  divi/row  divi/column  divi/group  divi/group-carousel
Content:     divi/text  divi/heading  divi/button  divi/image  divi/blurb  divi/cta
             divi/testimonial  divi/video  divi/code  divi/divider  divi/icon
             divi/icon-list (+item)  divi/link  divi/team-member  divi/number-counter
             divi/circle-counter  divi/counters (+counter)  divi/signup
Interactive: divi/accordion (+item)  divi/toggle  divi/tabs (+tab)
             divi/contact-form (+field)  divi/dropdown
Media:       divi/gallery  divi/slider (+slide)  divi/video-slider (+item)
             divi/lottie  divi/audio  divi/before-after-image
Dynamic:     divi/blog  divi/post-carousel  divi/post-slider  divi/post-title
             divi/portfolio  divi/filterable-portfolio  divi/comments  divi/pagination
Pricing:     divi/pricing-tables (+pricing-table)
Social:      divi/social-media-follow (+network)
Other:       divi/countdown-timer  divi/map  divi/menu  divi/search  divi/sidebar
             divi/login  divi/canvas-portal  divi/fullwidth-header  divi/post-nav
```

### Self-Closing vs Container

**Self-closing** (end with ` /-->`):
text, heading, button, image, blurb, cta, testimonial, video, code, divider, icon, link, team-member, number-counter, circle-counter, gallery, lottie, audio, before-after-image, countdown-timer, map, menu, search, sidebar, login, canvas-portal, fullwidth-header, post-nav, accordion-item, toggle, tab, contact-field, slide, video-slider-item, counter, pricing-table, social-media-follow-network, icon-list-item, signup, blog, post-carousel, post-slider, portfolio, filterable-portfolio, comments, pagination, dropdown

**Container** (require open + close tags):
section, row, column, group, group-carousel, accordion, tabs, contact-form, counters, pricing-tables, social-media-follow, icon-list, slider, video-slider

### Column Width (flexType) Cheat Sheet

```
24_24 = 100%    12_24 = 50%    8_24 = 33.3%    6_24 = 25%
16_24 = 66.7%   18_24 = 75%    4_24 = 16.7%
```

### `flexColumnStructure` Values

```
equal-columns_1   equal-columns_2   equal-columns_3   equal-columns_4
```

### Most-Used Style Property Paths

| Property | Path |
|----------|------|
| Background color | `module.decoration.background.desktop.value.color` |
| Section padding | `module.decoration.spacing.desktop.value.padding` |
| Border radius | `module.decoration.border.desktop.value.radius` |
| Box shadow | `module.decoration.boxShadow.desktop.value` |
| Max width | `module.decoration.sizing.desktop.value.maxWidth` |
| Column width | `module.decoration.sizing.desktop.value.flexType` |
| Row flex wrap | `module.decoration.layout.desktop.value.flexWrap` |
| Row col structure | `module.advanced.flexColumnStructure.desktop.value` |
| Heading text | `title.innerContent.desktop.value` (plain text) |
| Heading level | `title.decoration.font.font.desktop.value.headingLevel` |
| Body font color | `content.decoration.bodyFont.body.font.desktop.value.color` |
| Text alignment | `...font.desktop.value.textAlign` |
| Admin label | `module.meta.adminLabel.desktop.value` |
| Z-index | `module.decoration.zIndex.desktop.value` |

### Breakpoints

```
desktop     ≥ 981px   (always required)
tablet      768–980px
phoneWide   480–767px
phone       < 480px
```

### Variable Reference Syntax

```
Color:     "$variable({\"type\":\"color\",\"value\":{\"name\":\"gcid-ID\",\"settings\":{}}})$"
Size/Font: "$variable({\"type\":\"content\",\"value\":{\"name\":\"gvid-ID\",\"settings\":{}}})$"
Font fam:  "$variable({\"type\":\"content\",\"value\":{\"name\":\"--et_global_heading_font\",\"settings\":{}}})$"
```

### Nesting Hierarchy

```
JSON Root
  └── data["11"] = "PAGE MARKUP STRING"
        └── <!-- wp:divi/placeholder -->
              └── SECTION(s)
                    └── ROW(s)
                          └── COLUMN(s)
                                └── MODULE(s) or GROUP
                                      └── MODULE(s)
                                      └── Nested ROW → COLUMN → MODULE
              └── <!-- /wp:divi/placeholder -->
```

---

*DIVI5 JSON Skill — Version 0.2.0 | Based on DIVI5 Builder Version 5.0.1*
*Sources: DIVI5-SKILL.md v0.1.0 + Direct source code inspection of DIVI5 theme (PortabilityPost.php, Layout.php, HeadingModule.php, module.json definitions, conversion-outline.json files)*
